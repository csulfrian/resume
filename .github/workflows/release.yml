on:
  push:
    tags:
      - '*'                                                 # (1)

name: Upload Release Asset

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Remove swag line                              # (2)
        run: sed -i '1d' README.md
      - name: Build PDF from Markdown                       # (3)
        uses: BaileyJM02/markdown-to-pdf@v1
        with:
          input_dir: ./
          output_dir: ./
      - name: Rename pdf
        run: cp ./README.pdf ./christopher_sulfrian_resume.pdf
      - name: Create Release                                # (4)
        id: create_release
        uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
          files: ./christopher_sulfrian_resume.pdf
#      - name: Upload Release Asset                          # (5)
#        id: upload-release-asset
#        uses: actions/upload-release-asset@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          upload_url: ${{ steps.create_release.outputs.upload_url }}
#          asset_path: ./christopher_sulfrian_resume.pdf
#          asset_name: christopher_sulfrian_resume.pdf
#          asset_content_type: application/pdf

